//alert("hello");
// can do maths (22-4)=18 etc
/*
var tshirt = 10; 
var socks = 8; 
var plates = 20; 
var Total = tshirt + socks + plates; 
document.body.innerHTML = "Total: $" + Total;
*/

/*
var amountLeft = 100;
var lunch = 20;
var dinner = 50;
var salary = 200;
var currentSavings = amountLeft + salary - dinner - lunch;
document.body.innerHTML = " Current Amount: $" + currentSavings;
*/

// first, brackets; multiply & divide("*, /"); then ("+, -"); left to right solution in + -;
// (20.95+(7.99*2))*0.1 = 3.693// use (2095 + 799)/ 100 = 29.94 to avoid foat value error
// Math.round(); used to round of a number; ex - 
//var result = Math.round((2095 + 799) * 0.1); 
//document.body.innerHTML = "Result: $"+ result +" only";
//output = $289 

// STRINGS
/*'hello' is a string 
alet('hello'); will createe a string pop up of hello.
'jyoti' + 'rachas' (sum of strings called concatination)
jyotirachas (result)
typeof(" ") is use to check the data type
Type Cohersion: {automatic type conversion}
'hello' + 3 (3 will be converted into string automaticaly )
'hello3' (Result)
String also use order of operations means follow rule of ()'s
ex :
'$' + (20.95 + 7.99);
'$28.939999999999998' (result is in flotes)
so, to awoid number after calculations in flotes 
'$' + ((2095 + 799)/100); (calculate without decimal)
'28.94'
"I'm lerning" or use escape character
(\ just text) 'I\'m lerning' 
(\" = double quotes that is just text)
(\n = new line character)
`hello` (by backtick) {called templet strings} {Interpolation = insert value directly into a string}
`Items($(1+1)):$${(2095 + 799)/100} (uses interpolation)
'Items(' + (1 + 1) + '): $' + (2095 + 799)/100 (uses concatenation)
`sum
text` {multi line feature of templete trings}
*/


